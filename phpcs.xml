<?xml version="1.0"?>
<ruleset name="Aereus coding standard">
    <description>Aereus coding standard</description>

    <!-- display progress -->
    <arg value="p"/>
    <arg name="colors"/>
    <arg name="extensions" value="php"/>

    <!-- report - this causes the phpvs to fail so we need to pass the file in the
    command line for now. -->
    <!-- <arg name="report" value="checkstyle"/>
    <arg name="report-file" value=".reports/checkstyle.xml"/> -->

    <!-- inherit rules from: -->
    <rule ref="PSR2"/>
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
    <rule ref="Squiz.WhiteSpace.OperatorSpacing">
        <properties>
            <property name="ignoreNewlines" value="true"/>
        </properties>
    </rule>
    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace">
        <properties>
            <property name="ignoreBlankLines" value="false"/>
        </properties>
    </rule>
    <rule ref="PSR1.Files.SideEffects">
        <exclude-pattern>public/index.php</exclude-pattern>
    </rule>

    <!-- Paths to check -->
    <file>config</file>
    <file>src/Netric</file>
    <file>public/index.php</file>
</ruleset>
